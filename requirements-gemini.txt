# Gemini/Vertex だけを使う軽量バックエンド用（Docker 不要のネイティブ実行向け）。
# OpenCV/Tesseract を含まないため、ビルドが速く依存トラブルが起きにくい。
# ブラウザ内 OCR にフォールバックする前提なので、サーバー側の Tesseract は使わない。
# 共通依存（FastAPI / google-auth / requests）は base から取り込む。
-r requirements-base.txt
