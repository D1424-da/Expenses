# 全デプロイ共通の基盤依存（Web サーバ）。requirements*.txt から -r で取り込む。
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32
# ID トークン検証(任意)・Vertex 認証で使用。
google-auth==2.43.0
requests==2.32.5
# Stripe サブスクリプション
stripe==11.4.1
# Firebase Admin SDK（Webhook → Firestore 書き込み）
firebase-admin==6.8.0
